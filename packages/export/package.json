{
  "name": "@prune/export",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "validate:fixtures": "npm run build && node scripts/validate-export-fixtures.mjs",
    "test:interop": "npm run build && node scripts/test-interop.mjs",
    "test:utils": "npm run build && node scripts/test-utils.mjs"
  }
}
